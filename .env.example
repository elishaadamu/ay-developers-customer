# Environment Variables for Customer Dashboard

# Encryption key for localStorage data encryption
# IMPORTANT: Change this to a strong, unique key in production
# You can generate a secure key using: openssl rand -base64 32
VITE_ENCRYPTION_KEY=your-secret-encryption-key-change-in-production

# API Configuration
VITE_API_BASE_URL=http://localhost:3000/api

# Add other environment variables as needed
